<h1><%= @cocktail.name.capitalize %></h1>


<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.input :ingredient, collection: Ingredient.all %>
  <%= f.submit %>
<% end %>

<%= link_to "Back to menu", cocktail_path(@cocktail) %>
